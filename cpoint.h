#ifndef CPOINT_H
#define CPOINT_H

using namespace std;


class cPoint
{
public:
    cPoint();
    float x;
    float y;
    cPoint(float xx,float yy):x(xx),y(yy){}
};

#endif // CPOINT_H
